{
    int_constants: {
        API_VERSION_MAJOR: "DB_API_VERSION_MAJOR",
        API_VERSION_MINOR: "DB_API_VERSION_MINOR",
        API_LEVEL: "DDB_API_LEVEL",
        PL_MAIN: "PL_MAIN",
        PL_SEARCH: "PL_SEARCH",
        IS_SUBTRACK: "DDB_IS_SUBTRACK",
        IS_READONLY: "DDB_IS_READONLY",
        HAS_EMBEDDED_CUESHEET: "DDB_HAS_EMBEDDED_CUESHEET",
        TAG_ID3V1: "DDB_TAG_ID3V1",
        TAG_ID3V22: "DDB_TAG_ID3V22",
        TAG_ID3V23: "DDB_TAG_ID3V23",
        TAG_ID3V24: "DDB_TAG_ID3V24",
        TAG_APEV2: "DDB_TAG_APEV2",
        TAG_VORBISCOMMENTS: "DDB_TAG_VORBISCOMMENTS",
        TAG_CUESHEET: "DDB_TAG_CUESHEET",
        TAG_ICY: "DDB_TAG_ICY",
        TAG_ITUNES: "DDB_TAG_ITUNES",
        TAG_MASK: "DDB_TAG_MASK",
    },
    string_constants: {
        DEFAULT_CTMAPPING: "DDB_DEFAULT_CTMAPPING",
    },
    functions: {
        get_output: {
            ret: 'DB_output_t*',
        },

        playback_get_pos: {
            ret: 'float',
        },

        playback_set_pos: {
            args: ['float'],
        },

        plt_get_count: {
            ret: 'int',
        },

		streamer_get_playing_track: {
			ret: 'DB_playItem_t*',
		},

		streamer_get_playing_track: {
			ret: 'DB_playItem_t*',
		},

		streamer_get_playpos: {
			ret: 'float',
		},

		streamer_ok_to_read: {
			ret: 'int',
			args: ['int'],
		},

		streamer_reset: {
			args: ['int'],
		},

		streamer_read: {
			ret: 'int',
			args: ['jscharbuffer', 'int'],
		},

		streamer_set_bitrate: {
			args: ['int'],
		},

		streamer_get_apx_bitrate: {
			ret: 'int',
		},

		streamer_get_current_fileinfo: {
		    ret: 'DB_fileinfo_t*',
		},

		streamer_get_current_playlist: {
		    ret: 'int',
		},

        // TODO: needs dsp_chain_next
        streamer_get_dsp_chain: {
            ret: 'ddb_dsp_context_t*',
        },

        streamer_set_dsp_chain: {
            args: ['ddb_dsp_context_t*'],
        },

        streamer_dsp_refresh: {
        },

        quit: {
        },

        plt_ref: {
            args: ['ddb_playlist_t*'],
        },

        plt_unref: {
            args: ['ddb_playlist_t*'],
        },

        plt_get_count: {
            ret: 'int',
        },

        // TODO: needs plt_get_next
        plt_get_head: {
            ret: 'DB_playItem_t*',
            args: ['int'],
        },

        plt_get_sel_count: {
            ret: 'int',
            args: ['int'],
        },

        plt_add: {
            ret: 'int',
            args: ['int','jsstring'],
        },

        plt_remove: {
            args: ['int'],
        },

        plt_clear: {
            args: ['ddb_playlist_t*'],
        },

        pl_clear: {
        },

        plt_set_curr: {
            args: ['ddb_playlist_t*'],
        },

        plt_set_curr_idx: {
            args: ['int'],
        },

        plt_get_curr: {
            ret: 'ddb_playlist_t*',
        },

        plt_get_curr_idx: {
            ret: 'int',
        },

        plt_move: {
            args: ['int', 'int'],
        },

        // TODO: callbacks instead of jsnull hack
        plt_save: {
            ret: 'int',
            args: ['ddb_playlist_t*', 'DB_playItem_t*', 'DB_playItem_t*', 'jsstring', 'jsnull', 'jsnull', 'jsnull']
        },

    },
}
